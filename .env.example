# Exemplo de variáveis de ambiente
# Copie para .env para usar localmente

# PostgreSQL (deixe vazio para usar JSON local)
# DATABASE_URL=postgresql://usuario:senha@host/database

# Porta do servidor (opcional)
# PORT=3000
